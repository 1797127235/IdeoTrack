# 主域名同时承载后端 API 和管理后台 Web：
#   - /admin*  → Next.js 管理后台（basePath /admin，见 web/next.config.ts）
#   - 其余     → 后端 API
# 管理后台挂在路径下，无需为 admin.ideotrack.cc.cd 单独配子域名 DNS。
ideotrack.cc.cd {
  handle /admin* {
    reverse_proxy web:3001
  }
  handle {
    reverse_proxy api:3000
  }
}
