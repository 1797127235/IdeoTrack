meta {
  name: 获取当前用户
  type: http
}

get {
  url: {{baseUrl}}/api/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  {{studentToken}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data.role: eq student
}
