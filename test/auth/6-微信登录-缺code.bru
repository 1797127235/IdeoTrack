meta {
  name: 微信登录-缺少code返回400
  type: http
}

post {
  url: {{baseUrl}}/api/auth/wechat/login
  body: json
  auth: none
}

body:json {
  {}
}

assert {
  res.status: eq 400
  res.body.success: eq false
}

docs {
  # 微信登录链路
  真实微信登录需要 wx.login 返回的 code（只能在小程序环境拿到），
  Bruno 无法模拟，所以这里只测参数校验。

  完整链路（已真机验证）：
  1. 小程序 wx.login 拿 code
  2. POST /api/auth/wechat/login { code }
  3. 后端调微信 code2session 换 openid
  4. 未绑定 → 返回 { needBind: true, openid }
  5. 用户输学号密码 → POST /api/auth/wechat/bind
  6. 绑定成功，返回 JWT
}
