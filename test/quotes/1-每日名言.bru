meta {
  name: 每日名言-学生
  type: http
}

get {
  url: {{baseUrl}}/api/quotes/daily
  body: none
  auth: bearer
}

auth:bearer {
  {{studentToken}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data.content: isString
}
