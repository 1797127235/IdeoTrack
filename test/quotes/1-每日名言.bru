meta {
  name: 每日名言-学生
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/quotes/daily
  body: none
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data.content: isString
}
