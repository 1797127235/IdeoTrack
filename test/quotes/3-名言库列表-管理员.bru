meta {
  name: 名言库列表-管理员
  type: http
}

get {
  url: {{baseUrl}}/api/quotes
  body: none
  auth: bearer
}

auth:bearer {
  {{adminToken}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data: isArray
}
