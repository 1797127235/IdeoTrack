meta {
  name: 名言库列表-管理员
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/quotes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data: isArray
}
