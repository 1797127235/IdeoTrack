meta {
  name: 删除名言
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/quotes/{{quoteId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
