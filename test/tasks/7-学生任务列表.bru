meta {
  name: 学生任务列表
  type: http
}

get {
  url: {{baseUrl}}/api/tasks/my?page=1&limit=20
  body: none
  auth: bearer
}

auth:bearer {
  {{studentToken}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data: isArray
}
